# Copie para .env e preencha. O .env NÃO vai para o git.
#
# A service_role key entra em tudo no banco, sem passar por RLS. Ela existe
# só nesta máquina: nunca no navegador, nunca num print, nunca num commit.
# Está no Supabase em Settings → API → service_role (secret).

SUPABASE_URL=https://SEU-PROJETO.supabase.co
SUPABASE_SERVICE_KEY=cole-a-service-role-aqui

# onde a sessão do WhatsApp fica guardada (faça backup desta pasta:
# perdê-la significa ler o QR code de novo no celular)
PASTA_SESSAO=./sessao

# balde do Storage onde as fotos, áudios e PDFs dos clientes são guardados
BUCKET=anexos

# de quanto em quanto tempo a ponte pergunta se há mensagem para enviar (ms)
INTERVALO_FILA=2000
